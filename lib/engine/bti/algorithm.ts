import type { Player, BTIReport } from './types'

// Stub — all tests will fail until this is implemented.
// See lib/engine/bti/__tests__/algorithm.test.ts for the full spec.
export function computeReport(_playerA: Player, _playerB: Player): BTIReport {
  throw new Error('computeReport: not yet implemented')
}
